/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ItemResponseDto } from '../../models/item-response-dto';

export interface GetItemById$Params {
  'cart-id': number;
  'item-id': number;
}

export function getItemById(http: HttpClient, rootUrl: string, params: GetItemById$Params, context?: HttpContext): Observable<StrictHttpResponse<ItemResponseDto>> {
  const rb = new RequestBuilder(rootUrl, getItemById.PATH, 'get');
  if (params) {
    rb.path('cart-id', params['cart-id'], {});
    rb.path('item-id', params['item-id'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ItemResponseDto>;
    })
  );
}

getItemById.PATH = '/carts/{cart-id}/items/{item-id}';
