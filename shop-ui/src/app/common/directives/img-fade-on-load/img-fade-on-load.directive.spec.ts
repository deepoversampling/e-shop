import { ImgFadeOnLoadDirective } from './img-fade-on-load.directive';

describe('ImgFadeOnLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgFadeOnLoadDirective();
    expect(directive).toBeTruthy();
  });
});
