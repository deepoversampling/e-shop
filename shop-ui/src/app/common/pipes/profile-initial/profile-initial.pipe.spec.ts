import { ProfileInitialPipe } from './profile-initial.pipe';

describe('ProfileInitialPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileInitialPipe();
    expect(pipe).toBeTruthy();
  });
});
