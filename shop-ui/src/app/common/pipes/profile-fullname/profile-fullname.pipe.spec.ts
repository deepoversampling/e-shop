import { ProfileFullnamePipe } from './profile-fullname.pipe';

describe('ProfileFullnamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfileFullnamePipe();
    expect(pipe).toBeTruthy();
  });
});
